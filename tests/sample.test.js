// Este é um teste de exemplo para garantir que o Jest está funcionando.
// Ele não testa a API de vdd, apenas valida o workflow.
describe('Ambiente de Teste', () => {
  it('deve retornar true se 1 + 1 for igual a 2', () => {
    expect(1 + 1).toBe(2);
  });
});